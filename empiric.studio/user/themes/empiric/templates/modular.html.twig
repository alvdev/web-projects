{% extends 'partials/base.html.twig' %}

{% block content %}
    <h1 class="text-8xl text-gray-900 font-bold">
        {{ page.title }}
    </h1>
    {{ content|raw }}
 
    {% for module in page.collection() %}
        {{ module.content|raw }}
    {% endfor %}
{% endblock %}
