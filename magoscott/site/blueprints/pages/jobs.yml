Title: Trabajos

sections:
  jobs:
    type: pages
    headline: Jobs
    template: job
    layout: table
    columns:
      year:
        value: "{{ page.yearFrom }} {{ page.yearTo }}"
    image: false
    sortBy: yearTo desc yearFrom desc


    
