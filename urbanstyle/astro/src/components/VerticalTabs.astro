<section class="mt-32">
    <div class="container" x-data="{ tab: 1 }">
        <h2 class="text-4xl lg:text-5xl text-balance text-black font-bold uppercase xl:m-0 leading-tight">
            Servicios de cartelería complementarios
        </h2>

        <div class="mt-24 flex gap-24">
            <div class="w-2/5">
                <div class="sticky top-28">
                    <div class="flex flex-wrap gap-4 lg:flex-col">
                        <button class="group" x-on:click="tab = 1" :class="tab === 1 ? 'tab-active' : 'tab'">
                            Diseño profesional de carteles
                            <span class="inline-block ml-3 group-hover:animate-arrow-move-up">🡪</span>
                        </button>

                        <button class="group" x-on:click="tab = 2" :class="tab === 2 ? 'tab-active' : 'tab'">
                            Impresión de carteles
                            <span class="inline-block ml-3 group-hover:animate-arrow-move-up">🡪</span>
                        </button>

                        <button class="group" x-on:click="tab = 3" :class="tab === 3 ? 'tab-active' : 'tab'">
                            Vinilos para cristales
                            <span class="inline-block ml-3 group-hover:animate-arrow-move-up">🡪</span>
                        </button>

                        <button class="group" x-on:click="tab = 4" :class="tab === 4 ? 'tab-active' : 'tab'">
                            Informe fotográfico
                            <span class="inline-block ml-3 group-hover:animate-arrow-move-up">🡪</span>
                        </button>
                        <button class="group" x-on:click="tab = 5" :class="tab === 5 ? 'tab-active' : 'tab'">
                            Mantenimiento y reposición
                            <span class="inline-block ml-3 group-hover:animate-arrow-move-up">🡪</span>
                        </button>
                    </div>
                </div>
            </div>

            <div class="w-3/5" x-show="tab === 1" x-transition:enter="">Tab 1</div>
            <div class="w-3/5" x-show="tab === 2" x-transition:enter="">Tab 2</div>
            <div class="w-3/5" x-show="tab === 3" x-transition:enter="">Tab 3</div>
            <div class="w-3/5" x-show="tab === 4" x-transition:enter="">Tab 4</div>
            <div class="w-3/5" x-show="tab === 5" x-transition:enter="">Tab 5</div>
        </div>
    </div>
</section>
