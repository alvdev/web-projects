{% set options = {"items": {"@page.children": "/legal"}, "order": {"dir": "asc"}} %}
{% set links = page.collection(options) %}

{% for link in links %}
    <a class="opacity-70 whitespace-nowrap hover:opacity-100"
       href="{{ link.url }}">
        {{ link.title }}
    </a>
{% endfor %}
