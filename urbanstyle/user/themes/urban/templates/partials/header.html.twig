{% set isTorn = page.home ? 'torn relative after:absolute after:w-full after:h-8 after:-bottom-6' : '' %}

<header class="{{ isTorn }}">
    {% block header_navigation %}
        {% include 'partials/navigation.html.twig' %}
    {% endblock %}

    {#     {% if page.home %}
        <img src="{{ url('theme://images/headers/pegando-carteles.webp') }}"
             alt="Pegada de carteles"
             class="aspect-video w-full h-[75vh]">
    {% endif %} #}
</header>
