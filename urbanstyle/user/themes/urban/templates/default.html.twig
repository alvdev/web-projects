{% extends 'partials/base.html.twig' %}

{% block content %}
    <main class="flex-1 prose prose-xl max-w-none">
        <div class="container mt-16">
            <h1>
                {{ page.title }}
            </h1>
            {{ content|raw }}
        </div>
    </main>
{% endblock %}
